Questions
  DS120 Week 11 Practice Questions
  Topic: Relationships, keys, JOINs, set operations, and database design

  Instructions:
  - Run 01_week11_setup_and_seed.sql first.
  - Then write your answers under each question.
  - Use database: ds120_week11_practice
*/

USE ds120_week11_practice;

/* SECTION A: Relationships and keys */

-- Q1. List all villages with their region names.
-- Hint: villages has region_id. regions has region_id.


-- Q2. Show each village with its population and water source type.
-- Include: village_name, population, source_type_name, survey_year.


-- Q3. Calculate the water access percentage for each village.
-- Formula: people_with_basic_access * 100.0 / population
-- Include: village_name, region_name, access_percent.


-- Q4. Show villages where the water access percentage is below 60%.
-- Include region_name, village_name, access_percent.


-- Q5. Show all regions and any villages that belong to them.
-- Use a LEFT JOIN so regions still appear even if no village is linked.


/* SECTION B: JOINs for analysis */

-- Q6. Show all service requests with the village and region name.
-- Include request_id, request_date, issue_type, status, households_affected, village_name, region_name.


-- Q7. Count the number of service requests by status.


-- Q8. Count the number of service requests by region and status.


-- Q9. Find the total households affected by region.
-- Sort from highest to lowest total.


-- Q10. Find villages that do not have any service request.
-- Hint: LEFT JOIN service_requests and filter where request_id IS NULL.


/* SECTION C: Composite keys, bridge tables, and design */

-- Q11. Show each village and the service types available in that village.
-- Use village_services and service_types.


-- Q12. Find villages where water service coverage is below 50%.
-- Use the village_services table and service_type_name = 'Water'.


-- Q13. Explain using a query result: why does village_services need a composite key?
-- Show village_id, service_type_id, coverage_percent.


/* SECTION D: Set operations */

-- Q14. Create one combined priority list using UNION.
-- List A: villages with access below 50%.
-- List B: villages with Open service requests.
-- Output columns: village_name, reason.


-- Q15. Repeat Q14 using UNION ALL.
-- Observe how duplicate village names may appear if they meet both conditions.


-- Q16. Build a country comparison list using UNION.
-- Part 1: country names from adb_country_access where access percent is below 65.
-- Part 2: region names from ds120 regions where average village access is below 65.
-- Output columns: area_name, source_label.


/* SECTION E: Challenge */

-- Q17. Recreate an access_to_basic_services style report.
-- Include: region_name, village_name, population, source_type_name,
-- people_with_basic_access, access_percent, request_status if available.


-- Q18. Create a priority report.
-- Include only villages where access is below 60% OR there is an Open service request.
-- Include: region_name, village_name, access_percent, open_request_count, priority_label.
-- Suggested priority label:
--   High Priority = access below 50% and at least one open request
--   Monitor = access below 60% or at least one open request
--   Stable = otherwise

